import React from "react";
import "./styles.css";

const Header = () => (
  <header className="header" id="mainHeader">
    <h1 className="header__title">Main Header</h1>
  </header>
);

export default Header;
